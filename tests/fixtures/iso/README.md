# ISO Fixtures

Place local Windows ISO, WIM, ESD, or SWM test media here when running fixture
or UI automation scenarios.

These files are ignored by git and must not be committed. Use representative
names that include architecture markers where possible, for example:

```text
Win11_25H2_amd64_fixture.iso
Win11_25H2_arm64_fixture.iso
```
