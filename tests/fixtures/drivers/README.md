# Driver Fixtures

Place local driver test payloads here, including:

- Extracted folders containing `.inf` files.
- Vendor `.msi` driver bundles.
- Driver `.zip` archives.
- Small synthetic driver trees used by non-destructive tests.

These payloads are ignored by git. Product driver assets, if any, belong under
`assets\drivers\`; test-only driver inputs belong here.
