# YASB Preset

`config.yaml` and `styles.css` are the curated WinMint YASB preset files.
`preset.manifest.json` declares which files are staged into the FirstLogon
agent payload.

To update the shipped YASB setup, edit the preset files here and keep the
manifest file list in sync.
