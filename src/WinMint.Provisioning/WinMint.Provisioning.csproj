<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- PackageManager WinRT APIs for ticket 13 AppX safety net -->
    <TargetFramework>net11.0-windows10.0.19041.0</TargetFramework>
    <!-- Day-one: AOT on publish only; build uses IsAotCompatible from Directory.Build.props -->
    <PublishAot Condition="'$(Configuration)' == 'Release'">true</PublishAot>
    <!-- LibraryImport generator emits unsafe marshalling helpers -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="WinMint.Tests" />
  </ItemGroup>
</Project>
